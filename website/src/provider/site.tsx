"use client";

// import { makeLog } from "@/src/lib/logger";
// import figlet from "figlet";

export default function SiteProvider() {
  // figlet.defaults({ fontPath: "fonts/figlet" });
  // figlet.preloadFonts(["Standard"], ready);

  // const log = makeLog();
  // function ready() {
  //   console.log(figlet.textSync("TOAKIRYU"));
  // }

  return null;
}
